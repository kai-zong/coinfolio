Must force login only when identity is required (Use requireAuth for this)

Must have an Auth Debugger page that shows the authentication token (check out the class material)

Must allow users to change their personal information

The details page allows users to view a detailed view for each item.

Must be mapped to /details/{unique identifier} or /details?identifier={unique identifier} where unique identifier uniquely identifies the item being displayed (this can be transactions)

Must have /ping endpoint

Web application must be usable on a desktop, tablet, or phone

Web pages must be responsive at any width of the browser

Include accessibility reports from 3 pages using https://developers.google.com/web/tools/lighthouse

Your application should have at least 3 unit tests.

